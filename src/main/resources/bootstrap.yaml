spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: dev
        group: pms
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        group: pms
        file-extension: yaml
        shared-configs:
          - dataId: common.yaml
            refresh: true
            group: pms
          - dataId: mysql.yaml
            refresh: true
            group: pms
          - dataId: redis.yaml
            refresh: true
            group: pms

